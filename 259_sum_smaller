class Solution:
    def addDigits(self, num: int) -> int:
        if not len(str(num)) == 1:
            num = sum([eval(x) for x in str(num)])
            return self.addDigits(num)
        else:
            return num
